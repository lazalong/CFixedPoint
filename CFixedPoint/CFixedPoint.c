// FixedPointC.cpp : Defines the functions for the static library.
//
#define CFIXEDPOINT_IMPLEMENTATION
#include "CFixedPoint.h"

/*
namespace cfp
{
    int fp::toString()
    {
        return 5;
    }

    // C++ needs to define static members externally.
    fp::constructor fp::cons;
    const fp fp::min = fp(LONG_MIN);
    const fp fp::max = fp(LONG_MAX);
}
*/